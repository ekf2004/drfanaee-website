# Copy this file to .env.local and fill in real values.
# .env.local is gitignored and used for local dev.
# For Vercel, set the same variables in Project Settings → Environment Variables.

# Google Places API key (for live Google reviews on the site)
# Restrict in Google Cloud Console to: drfanaee.com, *.vercel.app, localhost — Places API only
VITE_GOOGLE_PLACES_KEY=
